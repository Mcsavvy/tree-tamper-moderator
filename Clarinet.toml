[project]
name = "tree-tamper-moderator"
authors = ["Forest Conservation Tech Team"]
description = "A decentralized ecosystem monitoring system for detecting and preventing unauthorized forest activities"
telemetry = false
requirements = []
[contracts.forest-guardian]
path = "contracts/forest-guardian.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
